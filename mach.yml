ver: 0 # ver
name: test
help: help string
pre: rust
env:
    shell: /bin/bash
commands:
    run:
        cmd:
        hlp:
    doc:
        open:
            cmd:
            hlp:
        cmd:
        hlp:
bootstrap: |
    sudo apt update
    sudo apt install firefox
build: |

